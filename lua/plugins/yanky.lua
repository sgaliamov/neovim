return {
  'gbprod/yanky.nvim',
  event = 'VeryLazy', -- to definitely load in vscode
  opts = {
    highlight = { timer = 250 },
  },
  -- keys = {} -- this keys overriding breaks default bindings.
}
